[pytest]
# Keeping this file (rather than root pyproject.toml) scopes pytest's
# rootdir to tests/, away from the repo root's __init__.py (the badge
# package entry point) - pytest otherwise treats a root __init__.py as
# a package it must import, which fails outside the badge runtime.
addopts = --import-mode=importlib
